---
outline: deep
---

# 环境搭建

## anaconda安装

<CardLink
  to="https://www.anaconda.com/"
  title="Anaconda"
  description="Anaconda官方下载链接"
  imageSrc="https://github.com/KB-talk/picx-images-hosting/raw/master/img/anaconda.2v0uyxdhjug0.jpg"
  imagePosition="right"
/>

::: tip

anaconda windows和Linux版本的安装和使用方法详情参考[anaconda常用使用方法](../../rk3588/0.开发环境搭建/2.开发工具.md##anaconda)

:::



::: details anaconda使用方法


1. 创建新环境

使用python3.8,创建名为rknn环境。

```
conda create -n rknn python=3.8
```

2. 开启conda

```
conda init
```

此操作是初始化conda，开启后可以使用终端执行conda命令。

3. 切换环境

切换到rknn环境中。
```
(base) zkb@ubuntu:~/桌面$ conda activate rknn
(rknn) zkb@ubuntu:~/桌面$ 
```

默认是在base环境中，切换到rknn环境。

4. 下载软件包

```
conda install <软件名>
pip install <软件名>
```

通常推荐使用conda安装库，因为它会自动处理依赖关系。但在某些情况下，如果conda中没有可用的包版本，也可以使用pip进行安装。

5. 删除软件包

```
conda remove <软件名>
```
6. 添加下载源

7. 查看存在的环境

```
conda env list
```

8. 查看软件包

```
conda list
```

9. 删除环境

```
conda activate base
conda env remove -n <环境名>
```

10. 退出环境

```
conda deactivate
```
:::

## pycharm安装

<CardLink
  to="https://www.jetbrains.com/pycharm/download/"
  title="pycharm"
  description="pycharm官方下载链接"
  imageSrc="https://KB-talk.github.io/picx-images-hosting/img/pycharm.2fq5fu2zcvms.jpg"
  imagePosition="right"
/>

## pytorch安装

<CardLink
  to="https://pytorch.org/"
  title="pytorch"
  description="pytorch官网"
  imageSrc="https://KB-talk.github.io/picx-images-hosting/pytorch/image.3yef52n4b6.webp"
  imagePosition="right"
/>

1. 获取GPU的CUDA版本。

打开cmd终端，输入nvidia-smi。

```
nvidia-smi
```

根据cuda版本号，选择对应的安装包。

例如我的版本号为12.6。安装指令如下。
```
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
```